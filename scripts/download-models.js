const { pipeline, env } = require('@huggingface/transformers'); const path = require('path'); env.cacheDir = path.join(process.cwd(), 'public/models'); env.allowRemoteModels = true; async function download() { console.log('Downloading model to', env.cacheDir); await pipeline('text2text-generation', 'Xenova/LaMini-Flan-T5-77M', { dtype: 'q8', device: 'cpu' }); console.log('Download complete!'); } download().catch(console.error);
