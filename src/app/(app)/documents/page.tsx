import { PhasePlaceholder } from "@/components/PhasePlaceholder";

export default function DocumentsIndexPage() {
  return (
    <PhasePlaceholder
      title="Document viewer"
      phase="Phase 2–4"
      summary="Open a document from a case after upload exists. Viewer, OCR fields, and risk indicators are later phases."
    />
  );
}
